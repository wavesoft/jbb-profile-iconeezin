
# Metadata
@uuid: 2
@revision: 0
@require:
    IconzeenAPI: iconeezin/api

# Objects

IconzeenAPI.AudioFile:
    init: [ url ]
    embed:
        - url
    properties:
        - url

IconzeenAPI.ExperimentFile:
    init: [ url, className ]
    embed:
        - url
    properties:
        - url
        - className
