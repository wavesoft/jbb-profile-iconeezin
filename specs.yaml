
# Metadata
@uuid: 2
@revision: 0
@require.encode:
    Iconeezin.API: iconeezin/api
@require.decode:
    Iconeezin: iconeezin

# Objects

Iconeezin.API.AudioFile:
    init: [ url ]
    embed:
        - url
    properties:
        - url

Iconeezin.API.ExperimentFile:
    init: [ url, className ]
    embed:
        - url
    properties:
        - url
        - className
